#ifndef ONE_TWO_H
#define ONE_TWO_H

#include <stdint.h>
#include <stdbool.h>

// 函数声明
void one(void);          // 执行一段动作并向左转
void two(void);          // 执行一段动作并向右转
void one_back(void);     // 掉头后执行一段动作并向右转
void two_back(void);     // 掉头后执行一段动作并向左转

#endif // ONE_TWO_H
